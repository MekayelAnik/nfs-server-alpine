# =============================================================================
# nfs-server-alpine Docker Build & Publish
# =============================================================================
# Monitors Alpine Linux edge/main repository for new nfs-utils package versions.
# Builds multi-arch Docker images when a new nfs-utils version is detected.
#
# Triggers:
#   - repository_dispatch (cron-job.org external trigger)
#   - workflow_dispatch (manual)
#
# Platforms: linux/amd64, linux/386, linux/arm64, linux/arm/v7, linux/arm/v6,
#            linux/riscv64, linux/ppc64le, linux/s390x
# Registries: GHCR + Docker Hub
#
# Required Secrets:
#   - DOCKERHUB_USERNAME
#   - DOCKERHUB_TOKEN
# Note: GITHUB_TOKEN is automatic with packages:write
# =============================================================================

name: Docker Build & Publish - NFS Server Alpine

on:
  repository_dispatch:
    types: [nfs-server-alpine-build]

  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - auto-check
          - build
          - promote-latest
          - force-promote-latest
          - retag-latest
          - sync-registries
        default: auto-check
      nfs_version:
        description: 'Override nfs-utils version (e.g. 2.7.1-r0)'
        required: false
        type: string
      force_build:
        description: 'Force build even if tag exists'
        required: false
        type: boolean
        default: false
      tag_as_latest:
        description: 'Tag as latest after build'
        required: false
        type: boolean
        default: true
      retag_source:
        description: 'Existing tag to promote as latest (for retag-latest action)'
        required: false
        type: string
      push_to_dockerhub:
        description: 'Push to Docker Hub'
        required: false
        type: boolean
        default: true
      push_to_ghcr:
        description: 'Push to GHCR'
        required: false
        type: boolean
        default: true
      platforms:
        description: 'Build platforms'
        required: false
        type: choice
        options:
          - 'linux/amd64,linux/386,linux/arm64,linux/arm/v7,linux/arm/v6,linux/riscv64,linux/ppc64le,linux/s390x'
          - 'linux/amd64,linux/arm64'
          - 'linux/amd64'
        default: 'linux/amd64,linux/386,linux/arm64,linux/arm/v7,linux/arm/v6,linux/riscv64,linux/ppc64le,linux/s390x'
      compression_level:
        description: 'ZSTD compression level'
        required: false
        type: choice
        options: ['22', '19', '15', '10', '5', '3']
        default: '22'
      run_security_scan:
        description: 'Run Trivy security scan'
        required: false
        type: boolean
        default: true

concurrency:
  group: nfs-build-${{ github.event_name }}-${{ github.event.inputs.action || 'auto-check' }}
  cancel-in-progress: false

permissions:
  contents: write
  packages: write
  actions: read
  security-events: write

env:
  TZ: ${{ vars.TZ || 'Asia/Dhaka' }}
  DOCKERHUB_REPO: ${{ vars.DOCKERHUB_REPO || 'mekayelanik/nfs-server-alpine' }}
  GHCR_REPO: ${{ vars.GHCR_REPO || format('ghcr.io/{0}/nfs-server-alpine', github.repository_owner) }}
  DEFAULT_PLATFORMS: ${{ vars.DEFAULT_PLATFORMS || 'linux/amd64,linux/386,linux/arm64,linux/arm/v7,linux/arm/v6,linux/riscv64,linux/ppc64le,linux/s390x' }}
  DEFAULT_BASE_IMAGE: ${{ vars.DEFAULT_BASE_IMAGE || 'alpine:edge' }}
  # repository_dispatch never populates github.event.inputs, so a payload like
  # {"client_payload":{"force_build":"true"}} was silently ignored. Accept both.
  FORCE_BUILD: ${{ github.event.inputs.force_build || github.event.client_payload.force_build || 'false' }}
  # Rebuild even with no upstream version change once the published image is this old,
  # so Alpine package updates actually reach the image.
  # Resolution order: client_payload > repo variable > default. Resolved in shell,
  # since `x != null && x || y` collapses an explicit `false` back to `y`.
  #
  #   {"event_type":"...","client_payload":{"package_refresh":false,"max_image_age_days":14}}
  CP_PACKAGE_REFRESH: ${{ github.event.client_payload.package_refresh }}
  CP_MAX_IMAGE_AGE_DAYS: ${{ github.event.client_payload.max_image_age_days }}
  VAR_PACKAGE_REFRESH: ${{ vars.PACKAGE_REFRESH_ENABLED }}
  VAR_MAX_IMAGE_AGE_DAYS: ${{ vars.MAX_IMAGE_AGE_DAYS }}

jobs:
  # ===========================================================================
  # Job 1: Check upstream releases
  # ===========================================================================
  check-releases:
    timeout-minutes: 15
    name: Check Alpine NFS Version
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: read
    outputs:
      should_build: ${{ steps.final-build-decision.outputs.should_build || steps.determine.outputs.should_build }}
      nfs_version: ${{ steps.determine.outputs.nfs_version }}
      image_tag: ${{ steps.determine.outputs.image_tag }}
      date_tag: ${{ steps.determine.outputs.date_tag }}
      action_type: ${{ steps.set-action.outputs.action }}
      platforms: ${{ steps.set-platforms.outputs.platforms }}
      compression_level: ${{ steps.set-compression.outputs.level }}
      build_matrix: ${{ steps.generate-matrix.outputs.matrix }}
      latest_needs_promote: ${{ steps.determine.outputs.latest_needs_promote }}
      stale_rebuild: ${{ steps.determine.outputs.stale_rebuild }}

    steps:
      - name: Set action type
        id: set-action
        run: |
          if [[ "${{ github.event_name }}" == "repository_dispatch" ]]; then
            echo "action=auto-check" >> "$GITHUB_OUTPUT"
          else
            echo "action=${{ github.event.inputs.action || 'auto-check' }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Set platforms
        id: set-platforms
        run: |
          PLATFORMS="${{ github.event.inputs.platforms || env.DEFAULT_PLATFORMS }}"
          echo "platforms=$PLATFORMS" >> "$GITHUB_OUTPUT"

      - name: Set compression level
        id: set-compression
        run: |
          LEVEL="${{ github.event.inputs.compression_level || '22' }}"
          echo "level=$LEVEL" >> "$GITHUB_OUTPUT"

      - name: Checkout
        uses: actions/checkout@v6

      - name: Fetch nfs-utils version from Alpine APKINDEX
        id: fetch-nfs
        run: |
          set -euo pipefail
          echo "::group::Fetching nfs-utils version from Alpine edge/main"

          NFS_VERSION=""

          for mirror in \
            "https://dl-cdn.alpinelinux.org/alpine" \
            "https://uk.alpinelinux.org/alpine" \
            "https://ftp.halifax.rwth-aachen.de/alpine"; do

            ALPINE_BRANCH="${DEFAULT_BASE_IMAGE#*:}"
            APKINDEX_URL="${mirror}/${ALPINE_BRANCH}/main/x86_64/APKINDEX.tar.gz"
            echo "Trying APKINDEX from: $APKINDEX_URL"

            NFS_VERSION=$(curl -fsSL --connect-timeout 10 --max-time 30 "$APKINDEX_URL" 2>/dev/null \
              | tar -xzO APKINDEX 2>/dev/null \
              | awk '/^P:nfs-utils$/{found=1} found && /^V:/{print substr($0,3); exit}' || true)

            if [[ -n "$NFS_VERSION" ]]; then
              echo "Found nfs-utils version $NFS_VERSION from APKINDEX"
              break
            fi
            echo "Mirror failed, trying next..."
          done

          if [[ -z "$NFS_VERSION" ]]; then
            echo "APKINDEX parsing failed, falling back to Alpine container query..."
            NFS_VERSION=$(docker run --rm "${DEFAULT_BASE_IMAGE}" sh -c \
              "apk update >/dev/null 2>&1 && apk info nfs-utils 2>/dev/null | head -1 | sed 's/^nfs-utils-//'" || true)
          fi

          if [[ -z "$NFS_VERSION" ]]; then
            echo "::error::Failed to determine nfs-utils version from Alpine repositories"
            echo "version=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "version=$NFS_VERSION" >> "$GITHUB_OUTPUT"
          echo "Alpine nfs-utils version: $NFS_VERSION"
          echo "::endgroup::"

      - name: Read pipeline state
        id: read-state
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "::group::Reading pipeline state"

          STATE_JSON=$(gh api "repos/${{ github.repository }}/contents/state.json?ref=pipeline-state" \
            --jq '.content' 2>/dev/null | base64 -d 2>/dev/null || echo '{}')

          STORED_NFS=$(echo "$STATE_JSON" | jq -r '.nfs_version // ""')

          echo "stored_nfs=$STORED_NFS" >> "$GITHUB_OUTPUT"
          echo "State: nfs=$STORED_NFS"
          echo "::endgroup::"

      - name: Determine build versions
        id: determine
        env:
          ACTION: ${{ steps.set-action.outputs.action }}
          FORCE_BUILD: ${{ env.FORCE_BUILD }}
          UPSTREAM_NFS: ${{ steps.fetch-nfs.outputs.version }}
          STORED_NFS: ${{ steps.read-state.outputs.stored_nfs }}
          MANUAL_NFS: ${{ github.event.inputs.nfs_version }}
          TAG_AS_LATEST: ${{ github.event.inputs.tag_as_latest || 'true' }}
        run: |
          set -euo pipefail
          echo "::group::Determining build versions"

          DATE_TAG=$(TZ="${TZ}" date +%d%m%Y)
          SHOULD_BUILD="false"
          LATEST_NEEDS_PROMOTE="false"
          STALE_REBUILD="false"

          NFS_VERSION="${MANUAL_NFS:-$UPSTREAM_NFS}"

          case "$ACTION" in
            auto-check)
              if [[ -n "$NFS_VERSION" && "$NFS_VERSION" != "$STORED_NFS" ]]; then
                echo "NFS version changed: $STORED_NFS → $NFS_VERSION"
                SHOULD_BUILD="true"
              fi

              if [[ "$NFS_VERSION" == "$STORED_NFS" || -z "$NFS_VERSION" ]]; then
                if [[ -n "$STORED_NFS" ]]; then
                  NFS_VERSION="$STORED_NFS"
                fi
              fi


              # Package refresh: upstream version changes are the only other trigger and
              # they are rare, so without this the image sits while Alpine ships security
              # updates underneath it. Uses skopeo, matching the rest of this step.
              PACKAGE_REFRESH="${CP_PACKAGE_REFRESH:-${VAR_PACKAGE_REFRESH:-true}}"
              MAX_IMAGE_AGE_DAYS="${CP_MAX_IMAGE_AGE_DAYS:-${VAR_MAX_IMAGE_AGE_DAYS:-30}}"
              if [[ "$SHOULD_BUILD" != "true" && "$PACKAGE_REFRESH" == "true" && "$MAX_IMAGE_AGE_DAYS" != "0" ]]; then
                CREATED=$(skopeo inspect --no-tags "docker://${{ env.GHCR_REPO }}:latest" 2>/dev/null | jq -r '.Created // empty' || echo "")
                if [[ -n "$CREATED" ]]; then
                  CREATED_EPOCH=$(date -d "$CREATED" +%s 2>/dev/null || echo 0)
                  if [[ "$CREATED_EPOCH" -gt 0 ]]; then
                    AGE_DAYS=$(( ( $(date +%s) - CREATED_EPOCH ) / 86400 ))
                    if [[ "$AGE_DAYS" -ge "$MAX_IMAGE_AGE_DAYS" ]]; then
                      echo "Published image is ${AGE_DAYS}d old (>= ${MAX_IMAGE_AGE_DAYS}d) -- rebuilding to pick up Alpine package updates"
                      SHOULD_BUILD="true"
                      STALE_REBUILD="true"
                    else
                      echo "Published image is ${AGE_DAYS}d old (< ${MAX_IMAGE_AGE_DAYS}d) -- no package-refresh rebuild"
                    fi
                  fi
                else
                  echo "::warning::Could not read the creation date of :latest -- skipping the image age check"
                fi
              elif [[ "$SHOULD_BUILD" != "true" ]]; then
                echo "Package-refresh rebuild disabled (package_refresh=$PACKAGE_REFRESH, max_image_age_days=$MAX_IMAGE_AGE_DAYS)"
              fi
              if [[ "$TAG_AS_LATEST" == "true" && "$SHOULD_BUILD" == "true" ]]; then
                LATEST_NEEDS_PROMOTE="true"
              elif [[ "$TAG_AS_LATEST" == "true" && -n "$NFS_VERSION" ]]; then
                IMAGE_TAG="${NFS_VERSION}"
                GHCR_PREFIX="${{ env.GHCR_REPO }}"
                VERSION_DIGEST=$(skopeo inspect --no-tags "docker://${GHCR_PREFIX}:${IMAGE_TAG}" 2>/dev/null | jq -r '.Digest // "none"' || echo "none")
                LATEST_DIGEST=$(skopeo inspect --no-tags "docker://${GHCR_PREFIX}:latest" 2>/dev/null | jq -r '.Digest // "missing"' || echo "missing")
                if [[ "$VERSION_DIGEST" != "none" && "$VERSION_DIGEST" != "$LATEST_DIGEST" ]]; then
                  LATEST_NEEDS_PROMOTE="true"
                fi
              fi
              ;;

            build)
              SHOULD_BUILD="true"
              LATEST_NEEDS_PROMOTE="true"
              ;;

            promote-latest|force-promote-latest)
              SHOULD_BUILD="false"
              LATEST_NEEDS_PROMOTE="true"
              if [[ -z "$NFS_VERSION" && -n "$STORED_NFS" ]]; then
                NFS_VERSION="$STORED_NFS"
              fi
              ;;

            retag-latest)
              SHOULD_BUILD="false"
              LATEST_NEEDS_PROMOTE="true"
              RETAG_SOURCE="${{ github.event.inputs.retag_source }}"
              if [[ -z "$RETAG_SOURCE" ]]; then
                echo "::error::retag-latest requires retag_source input"
                LATEST_NEEDS_PROMOTE="false"
              fi
              ;;

            sync-registries)
              SHOULD_BUILD="false"
              if [[ -z "$NFS_VERSION" && -n "$STORED_NFS" ]]; then
                NFS_VERSION="$STORED_NFS"
              fi
              ;;
          esac

          if [[ "$FORCE_BUILD" == "true" && "$ACTION" != "promote-latest" && "$ACTION" != "force-promote-latest" && "$ACTION" != "retag-latest" && "$ACTION" != "sync-registries" ]]; then
            SHOULD_BUILD="true"
            LATEST_NEEDS_PROMOTE="true"
          fi

          if [[ "$ACTION" == "retag-latest" ]]; then
            IMAGE_TAG="${RETAG_SOURCE}"
          else
            IMAGE_TAG="${NFS_VERSION}"
          fi

          if [[ "$ACTION" != "retag-latest" && -z "$NFS_VERSION" ]]; then
            echo "::error::Missing nfs-utils version"
            SHOULD_BUILD="false"
          fi

          echo "should_build=$SHOULD_BUILD" >> "$GITHUB_OUTPUT"
          echo "nfs_version=$NFS_VERSION" >> "$GITHUB_OUTPUT"
          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "date_tag=$DATE_TAG" >> "$GITHUB_OUTPUT"
          echo "latest_needs_promote=$LATEST_NEEDS_PROMOTE" >> "$GITHUB_OUTPUT"
          echo "stale_rebuild=$STALE_REBUILD" >> "$GITHUB_OUTPUT"

          echo "Build decision: should_build=$SHOULD_BUILD"
          echo "  nfs=$NFS_VERSION"
          echo "  tag=$IMAGE_TAG"
          echo "  date=$DATE_TAG"
          echo "::endgroup::"

      - name: Check if tag exists in GHCR
        id: check-existing
        if: steps.determine.outputs.should_build == 'true'
        env:
          IMAGE_TAG: ${{ steps.determine.outputs.image_tag }}
          FORCE_BUILD: ${{ env.FORCE_BUILD }}
          STALE_REBUILD: ${{ steps.determine.outputs.stale_rebuild }}
        run: |
          set -euo pipefail
          if [[ "${FORCE_BUILD:-}" == "true" || "${STALE_REBUILD:-}" == "true" ]]; then
            echo "Force build or package refresh — skipping existence check"
            echo "exists=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          DIGEST=$(skopeo inspect --no-tags "docker://${{ env.GHCR_REPO }}:${IMAGE_TAG}" 2>/dev/null | jq -r '.Digest // empty' || echo "")
          if [[ -n "$DIGEST" ]]; then
            echo "::warning::Tag ${IMAGE_TAG} already exists in GHCR (digest: $DIGEST)"
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Tag ${IMAGE_TAG} not found in GHCR — will build"
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Final build decision
        id: final-build-decision
        if: steps.determine.outputs.should_build == 'true'
        run: |
          # A package refresh reuses the same version tag on purpose.
          if [[ "${{ steps.check-existing.outputs.exists }}" == "true" \
                && "${{ env.FORCE_BUILD }}" != "true" \
                && "${{ steps.determine.outputs.stale_rebuild }}" != "true" ]]; then
            echo "Tag already exists and force_build not set — skipping build"
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_build=${{ steps.determine.outputs.should_build }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate build matrix
        id: generate-matrix
        env:
          PLATFORMS: ${{ steps.set-platforms.outputs.platforms }}
          SHOULD_BUILD: ${{ steps.final-build-decision.outputs.should_build || steps.determine.outputs.should_build }}
        run: |
          set -euo pipefail
          if [[ "$SHOULD_BUILD" != "true" ]]; then
            echo 'matrix=[]' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          ACTION_RUNNER="${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-24.04' }}"
          runner_suffix_for() {
            case "$1" in
              linux/amd64)    echo "" ;;
              linux/386)      echo "" ;;
              linux/arm64)    echo "-arm" ;;
              linux/arm/v7)   echo "-arm" ;;
              linux/arm/v6)   echo "-arm" ;;
              linux/riscv64)  echo "-arm" ;;
              *)              echo "" ;;
            esac
          }

          MATRIX="["
          FIRST=true
          IFS=',' read -ra PLAT_ARRAY <<< "$PLATFORMS"
          for PLAT in "${PLAT_ARRAY[@]}"; do
            PLAT=$(echo "$PLAT" | xargs)
            SLUG=$(echo "$PLAT" | sed 's|linux/||; s|/|-|g')
            SUFFIX=$(runner_suffix_for "$PLAT")
            RUNNER="${ACTION_RUNNER}${SUFFIX}"

            if [[ "$FIRST" != "true" ]]; then MATRIX+=","; fi
            MATRIX+="{\"platform\":\"$PLAT\",\"platform_slug\":\"$SLUG\",\"runner\":\"$RUNNER\"}"
            FIRST=false
          done
          MATRIX+="]"

          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"
          echo "Build matrix: $MATRIX"

  # ===========================================================================
  # Job 2: Build per-platform images
  # ===========================================================================
  build-platform:
    timeout-minutes: 120
    name: Build ${{ matrix.platform_slug }}
    needs: check-releases
    if: >-
      needs.check-releases.outputs.should_build == 'true'
      && needs.check-releases.outputs.build_matrix != '[]'
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        include: ${{ fromJson(needs.check-releases.outputs.build_matrix || '[]') }}

    concurrency:
      group: build-nfs-${{ matrix.platform_slug }}
      cancel-in-progress: false

    steps:
      - name: Free disk space
        if: matrix.platform_slug == 'amd64'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc 2>/dev/null || true
          df -h /

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup build environment
        uses: ./.github/actions/setup-build-env
        with:
          do_checkout: 'false'
          setup_qemu: ${{ matrix.platform_slug != 'amd64' && matrix.platform_slug != '386' && matrix.platform_slug != 'arm64' && matrix.platform_slug != 'arm-v7' && matrix.platform_slug != 'arm-v6' && 'true' || 'false' }}
          setup_buildx: 'true'
          login_dockerhub: 'true'
          login_ghcr: 'true'
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
          ghcr_actor: ${{ github.actor }}
          ghcr_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if platform tag exists
        id: check-tag
        env:
          FORCE_BUILD: ${{ env.FORCE_BUILD }}
          # Without this the job-level exemption is useless: every platform build skips
          # and the refresh produces nothing at all.
          STALE_REBUILD: ${{ needs.check-releases.outputs.stale_rebuild }}
          GHCR_REPO: ${{ env.GHCR_REPO }}
          VERSION: ${{ needs.check-releases.outputs.image_tag }}
        run: |
          set -euo pipefail
          if [[ "${FORCE_BUILD:-}" == "true" || "${STALE_REBUILD:-}" == "true" ]]; then
            echo "Force build or package refresh — skipping existence check"
            echo "skip_build=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          DIGEST=$(crane digest "${GHCR_REPO}:${VERSION}" 2>/dev/null || echo "")
          if [[ -n "$DIGEST" ]]; then
            echo "::notice::Tag ${VERSION} exists on ${GHCR_REPO} — skipping platform build"
            echo "skip_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip_build=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Prepare Dockerfile
        id: prepare
        if: steps.check-tag.outputs.skip_build != 'true'
        env:
          NFS_VERSION: ${{ needs.check-releases.outputs.nfs_version }}
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
          DATE_TAG: ${{ needs.check-releases.outputs.date_tag }}
        run: |
          set -euo pipefail

          bash ./DockerfileModifier.sh

          PUSH_TARGETS=""
          if [[ "${{ github.event.inputs.push_to_ghcr || 'true' }}" == "true" ]]; then
            PUSH_TARGETS="${{ env.GHCR_REPO }}"
          fi
          if [[ "${{ github.event.inputs.push_to_dockerhub || 'true' }}" == "true" ]]; then
            if [[ -n "$PUSH_TARGETS" ]]; then PUSH_TARGETS="$PUSH_TARGETS,"; fi
            PUSH_TARGETS="${PUSH_TARGETS}${{ env.DOCKERHUB_REPO }}"
          fi

          echo "push_targets=$PUSH_TARGETS" >> "$GITHUB_OUTPUT"
          echo "dockerfile=Dockerfile.nfs-server-alpine" >> "$GITHUB_OUTPUT"

      - name: Get base image digest
        id: base-digest
        if: steps.check-tag.outputs.skip_build != 'true'
        run: |
          BASE="${DEFAULT_BASE_IMAGE}"
          DIGEST=$(crane digest "$BASE" 2>/dev/null || echo "unknown")
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Determine cache reference
        id: cache-ref
        if: steps.check-tag.outputs.skip_build != 'true'
        run: |
          SLUG="${{ matrix.platform_slug }}"
          echo "tag=buildcache-${SLUG}" >> "$GITHUB_OUTPUT"
          echo "scope=nfs-v1-${SLUG}" >> "$GITHUB_OUTPUT"

      - name: Build and push by digest
        id: build-push
        if: steps.check-tag.outputs.skip_build != 'true'
        uses: ./.github/actions/build-push-retry
        with:
          context: .
          file: ${{ steps.prepare.outputs.dockerfile }}
          platforms: ${{ matrix.platform }}
          outputs: type=image,"name=${{ steps.prepare.outputs.push_targets }}",push-by-digest=true,name-canonical=true,push=true,compression=zstd,compression-level=${{ needs.check-releases.outputs.compression_level }},oci-mediatypes=true
          cache-from: |
            type=registry,ref=${{ env.GHCR_REPO }}:${{ steps.cache-ref.outputs.tag }}
            type=gha,scope=${{ steps.cache-ref.outputs.scope }}
          cache-to: type=gha,scope=${{ steps.cache-ref.outputs.scope }},mode=max
          provenance: mode=max
          sbom: true
          annotations: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.description=NFS Server on Alpine Linux
            org.opencontainers.image.licenses=GPL-3.0
            org.opencontainers.image.title=nfs-server-alpine
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.authors=MD. MEKAYEL ANIK <mekayel.anik@gmail.com>
            org.opencontainers.image.vendor=MD. MEKAYEL ANIK
            dev.mekayelanik.country-of-origin=Bangladesh
            dev.mekayelanik.message=== Free Palestine ==

      - name: Export digest
        if: steps.check-tag.outputs.skip_build != 'true'
        run: |
          mkdir -p /tmp/digests
          DIGEST="${{ steps.build-push.outputs.digest }}"
          touch "/tmp/digests/${DIGEST#sha256:}"
          echo "Exported digest: $DIGEST"

      - name: Upload digest artifact
        if: steps.check-tag.outputs.skip_build != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: digest-${{ needs.check-releases.outputs.image_tag }}-${{ matrix.platform_slug }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  # ===========================================================================
  # Job 2b: Merge platform manifests into multi-arch image
  # ===========================================================================
  merge-manifest:
    timeout-minutes: 120
    name: Merge Manifest
    needs: [check-releases, build-platform]
    if: ${{ !cancelled() && needs.check-releases.result == 'success' && needs.check-releases.outputs.should_build == 'true' }}
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: write
      security-events: write

    steps:
      - name: Download all digests
        id: download-digests
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digest-${{ needs.check-releases.outputs.image_tag }}-*
          merge-multiple: true

      - name: Check if merge is needed
        id: check-digests
        run: |
          if [[ -d /tmp/digests ]] && ls /tmp/digests/* >/dev/null 2>&1; then
            echo "has_digests=true" >> "$GITHUB_OUTPUT"
            echo "Found digests to merge:"
            ls -la /tmp/digests/
          else
            echo "has_digests=false" >> "$GITHUB_OUTPUT"
            echo "No digests found — all builds were skipped (tags already exist)"
          fi

      - name: Checkout
        if: steps.check-digests.outputs.has_digests == 'true'
        uses: actions/checkout@v6

      - name: Setup build environment
        if: steps.check-digests.outputs.has_digests == 'true'
        uses: ./.github/actions/setup-build-env
        with:
          do_checkout: 'false'
          setup_qemu: 'false'
          setup_buildx: 'true'
          login_dockerhub: 'true'
          login_ghcr: 'true'
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
          ghcr_actor: ${{ github.actor }}
          ghcr_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine image tags
        if: steps.check-digests.outputs.has_digests == 'true'
        id: tags
        env:
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
          DATE_TAG: ${{ needs.check-releases.outputs.date_tag }}
          TAG_AS_LATEST: ${{ github.event.inputs.tag_as_latest || 'true' }}
        run: |
          VERSION_TAG="${IMAGE_TAG}"
          DATED_TAG="${IMAGE_TAG}-${DATE_TAG}"

          GHCR_TAGS="${{ env.GHCR_REPO }}:${VERSION_TAG},${{ env.GHCR_REPO }}:${DATED_TAG}"
          DH_TAGS="${{ env.DOCKERHUB_REPO }}:${VERSION_TAG},${{ env.DOCKERHUB_REPO }}:${DATED_TAG}"

          if [[ "$TAG_AS_LATEST" == "true" ]]; then
            GHCR_TAGS="${GHCR_TAGS},${{ env.GHCR_REPO }}:latest"
            DH_TAGS="${DH_TAGS},${{ env.DOCKERHUB_REPO }}:latest"
          fi

          echo "ghcr_tags=$GHCR_TAGS" >> "$GITHUB_OUTPUT"
          echo "dh_tags=$DH_TAGS" >> "$GITHUB_OUTPUT"
          echo "version_tag=$VERSION_TAG" >> "$GITHUB_OUTPUT"
          echo "dated_tag=$DATED_TAG" >> "$GITHUB_OUTPUT"

      - name: Create GHCR multi-arch manifest
        if: steps.check-digests.outputs.has_digests == 'true'
        run: |
          set -euo pipefail
          DIGESTS=""
          for f in /tmp/digests/*; do
            DIGESTS="$DIGESTS ${{ env.GHCR_REPO }}@sha256:$(basename $f)"
          done

          IFS=',' read -ra TAGS <<< "${{ steps.tags.outputs.ghcr_tags }}"
          TAG_ARGS=""
          for TAG in "${TAGS[@]}"; do
            TAG_ARGS="$TAG_ARGS -t $TAG"
          done

          ANNO_ARGS=()
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.source=https://github.com/${{ github.repository }}")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.description=NFS Server on Alpine Linux")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.licenses=GPL-3.0")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.title=nfs-server-alpine")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.url=https://github.com/${{ github.repository }}")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.authors=MD. MEKAYEL ANIK <mekayel.anik@gmail.com>")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.vendor=MD. MEKAYEL ANIK")
          ANNO_ARGS+=(--annotation "index:dev.mekayelanik.country-of-origin=Bangladesh")
          ANNO_ARGS+=(--annotation "index:dev.mekayelanik.message=== Free Palestine ==")
          for attempt in 1 2 3 4 5; do
            echo "Attempt $attempt: Creating GHCR manifest..."
            if docker buildx imagetools create "${ANNO_ARGS[@]}" $TAG_ARGS $DIGESTS 2>&1; then
              echo "GHCR manifest created successfully"
              break
            fi
            if [[ $attempt -lt 5 ]]; then
              WAIT=$((30 * attempt))
              echo "::warning::Manifest create failed, retrying in ${WAIT}s..."
              sleep "$WAIT"
            else
              echo "::error::Failed to create GHCR manifest after 5 attempts"
              exit 1
            fi
          done

      - name: Create Docker Hub multi-arch manifest
        if: steps.check-digests.outputs.has_digests == 'true' && (github.event.inputs.push_to_dockerhub || 'true') == 'true'
        run: |
          set -euo pipefail
          SOURCE="${{ env.GHCR_REPO }}:${{ steps.tags.outputs.version_tag }}"

          IFS=',' read -ra TAGS <<< "${{ steps.tags.outputs.dh_tags }}"
          TAG_ARGS=""
          for TAG in "${TAGS[@]}"; do
            TAG_ARGS="$TAG_ARGS -t $TAG"
          done

          ANNO_ARGS=()
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.source=https://github.com/${{ github.repository }}")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.description=NFS Server on Alpine Linux")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.licenses=GPL-3.0")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.title=nfs-server-alpine")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.url=https://github.com/${{ github.repository }}")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.authors=MD. MEKAYEL ANIK <mekayel.anik@gmail.com>")
          ANNO_ARGS+=(--annotation "index:org.opencontainers.image.vendor=MD. MEKAYEL ANIK")
          ANNO_ARGS+=(--annotation "index:dev.mekayelanik.country-of-origin=Bangladesh")
          ANNO_ARGS+=(--annotation "index:dev.mekayelanik.message=== Free Palestine ==")
          for attempt in 1 2 3 4 5; do
            echo "Attempt $attempt: Creating Docker Hub manifest..."
            if docker buildx imagetools create "${ANNO_ARGS[@]}" $TAG_ARGS "$SOURCE" 2>&1; then
              echo "Docker Hub manifest created successfully"
              break
            fi
            if [[ $attempt -lt 5 ]]; then
              WAIT=$((30 * attempt))
              echo "::warning::Manifest create failed, retrying in ${WAIT}s..."
              sleep "$WAIT"
            else
              echo "::warning::Failed to create Docker Hub manifest after 5 attempts (non-fatal)"
            fi
          done

      - name: Run Trivy security scan
        if: steps.check-digests.outputs.has_digests == 'true' && (github.event.inputs.run_security_scan || 'true') == 'true'
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: '${{ env.DOCKERHUB_REPO }}:${{ steps.tags.outputs.version_tag }}'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          timeout: '10m'

      - name: Upload Trivy scan results
        if: steps.check-digests.outputs.has_digests == 'true' && (github.event.inputs.run_security_scan || 'true') == 'true'
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy-nfs-server-alpine'

  # ===========================================================================
  # Job 3: Sync registries
  # ===========================================================================
  sync-registries:
    timeout-minutes: 120
    name: Sync Registries
    needs: [check-releases, merge-manifest]
    if: >-
      always()
      && needs.check-releases.result == 'success'
      && (
        needs.check-releases.outputs.action_type == 'sync-registries'
        || needs.merge-manifest.result == 'success'
      )
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup build environment
        uses: ./.github/actions/setup-build-env
        with:
          do_checkout: 'false'
          setup_qemu: 'false'
          setup_buildx: 'false'
          login_dockerhub: 'true'
          login_ghcr: 'true'
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
          ghcr_actor: ${{ github.actor }}
          ghcr_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync tags between registries
        uses: ./.github/actions/registry-sync
        with:
          dockerhub_repo: ${{ env.DOCKERHUB_REPO }}
          ghcr_repo: ${{ env.GHCR_REPO }}
          tags: '${{ needs.check-releases.outputs.image_tag }},${{ needs.check-releases.outputs.image_tag }}-${{ needs.check-releases.outputs.date_tag }}'

  # ===========================================================================
  # Job 4: Promote to latest
  # ===========================================================================
  promote-latest:
    timeout-minutes: 120
    name: Promote Latest
    needs: [check-releases, merge-manifest]
    if: >-
      always()
      && needs.check-releases.result == 'success'
      && (
        needs.check-releases.outputs.action_type == 'promote-latest'
        || needs.check-releases.outputs.action_type == 'force-promote-latest'
        || needs.check-releases.outputs.action_type == 'retag-latest'
        || (
          needs.check-releases.outputs.action_type == 'auto-check'
          && needs.check-releases.outputs.latest_needs_promote == 'true'
          && (needs.merge-manifest.result == 'success' || needs.merge-manifest.result == 'skipped')
        )
        || (
          needs.check-releases.outputs.action_type == 'build'
          && (needs.merge-manifest.result == 'success' || needs.merge-manifest.result == 'skipped')
        )
      )
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: write
    outputs:
      promoted_version: ${{ steps.promote.outputs.promoted_version }}
      promoted_digest: ${{ steps.promote.outputs.promoted_digest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup build environment
        uses: ./.github/actions/setup-build-env
        with:
          do_checkout: 'false'
          setup_qemu: 'false'
          setup_buildx: 'false'
          login_dockerhub: 'true'
          login_ghcr: 'true'
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
          ghcr_actor: ${{ github.actor }}
          ghcr_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Promote to latest
        id: promote
        env:
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
          ACTION: ${{ needs.check-releases.outputs.action_type }}
        run: |
          set -euo pipefail
          source .github/scripts/lib-retry.sh

          GHCR_SRC="${{ env.GHCR_REPO }}:${IMAGE_TAG}"
          GHCR_DST="${{ env.GHCR_REPO }}:latest"
          DH_DST="${{ env.DOCKERHUB_REPO }}:latest"

          SRC_DIGEST=$(crane digest "$GHCR_SRC" 2>/dev/null || echo "")
          if [[ -z "$SRC_DIGEST" ]]; then
            echo "::error::Source tag ${IMAGE_TAG} not found in GHCR"
            exit 1
          fi

          LATEST_DIGEST=$(crane digest "$GHCR_DST" 2>/dev/null || echo "")
          if [[ "$SRC_DIGEST" == "$LATEST_DIGEST" && "$ACTION" != "force-promote-latest" && "$ACTION" != "retag-latest" ]]; then
            echo "Latest already points to ${IMAGE_TAG} (digest: $SRC_DIGEST) — skipping"
            echo "promoted_version=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
            echo "promoted_digest=$SRC_DIGEST" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Promoting ${IMAGE_TAG} → latest (GHCR)"
          for attempt in 1 2 3 4 5; do
            if crane copy "$GHCR_SRC" "$GHCR_DST" 2>&1; then
              echo "GHCR promotion successful"
              break
            fi
            if [[ $attempt -lt 5 ]]; then
              WAIT=$((10 * attempt))
              echo "::warning::crane copy failed, retrying in ${WAIT}s..."
              sleep "$WAIT"
            else
              echo "::error::Failed to promote to GHCR latest after 5 attempts"
              exit 1
            fi
          done

          echo "Promoting ${IMAGE_TAG} → latest (Docker Hub)"
          for attempt in 1 2 3; do
            if crane copy "$GHCR_SRC" "$DH_DST" 2>&1; then
              echo "Docker Hub promotion successful"
              break
            fi
            if [[ $attempt -lt 3 ]]; then
              WAIT=$((15 * attempt))
              echo "::warning::Docker Hub promotion failed, retrying in ${WAIT}s..."
              sleep "$WAIT"
            else
              echo "::warning::Failed to promote to Docker Hub latest (non-fatal)"
            fi
          done

          echo "promoted_version=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "promoted_digest=$SRC_DIGEST" >> "$GITHUB_OUTPUT"

  # ===========================================================================
  # Job 5: Update pipeline state
  # ===========================================================================
  update-state:
    timeout-minutes: 15
    name: Update Pipeline State
    needs: [check-releases, merge-manifest]
    if: >-
      always()
      && needs.check-releases.result == 'success'
      && needs.merge-manifest.result == 'success'
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: write

    steps:
      - name: Checkout pipeline-state branch
        uses: actions/checkout@v6
        with:
          ref: pipeline-state
          fetch-depth: 1
        continue-on-error: true

      - name: Create pipeline-state branch if needed
        run: |
          if ! git rev-parse --verify HEAD >/dev/null 2>&1; then
            git init
            git checkout --orphan pipeline-state
            git rm -rf --ignore-unmatch . 2>/dev/null || true
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git remote add origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" 2>/dev/null || true
            echo "Created orphan pipeline-state branch"
          fi

      - name: Write state
        env:
          NFS_VERSION: ${{ needs.check-releases.outputs.nfs_version }}
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
        run: |
          set -euo pipefail
          cat > state.json <<EOF
          {
            "nfs_version": "${NFS_VERSION}",
            "image_tag": "${IMAGE_TAG}",
            "updated_at": "$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          }
          EOF

          echo "${NFS_VERSION}" > nfs-version
          date -u +'%Y-%m-%dT%H:%M:%SZ' > last-built-time

      - name: Commit and push state
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet && { echo "No state changes"; exit 0; }
          git commit -m "Update pipeline state [skip ci]"
          for attempt in 1 2 3; do
            if git push origin pipeline-state 2>&1; then
              echo "State pushed successfully"
              exit 0
            fi
            git pull --rebase origin pipeline-state 2>/dev/null || true
            sleep $((5 * attempt))
          done
          echo "::warning::Failed to push pipeline state after 3 attempts"

  # ===========================================================================
  # Job 6: Update metadata on main branch
  # ===========================================================================
  update-metadata:
    timeout-minutes: 15
    name: Update Metadata
    needs: [check-releases, merge-manifest, promote-latest]
    if: >-
      always()
      && (needs.merge-manifest.result == 'success' || needs.promote-latest.result == 'success')
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update resource files
        env:
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
          NFS_VERSION: ${{ needs.check-releases.outputs.nfs_version }}
        run: |
          set -euo pipefail
          mkdir -p resources

          echo "${IMAGE_TAG}" > resources/tag
          date -u +'%Y-%m-%dT%H:%M:%SZ' > resources/build-timestamp
          echo "${NFS_VERSION}" > resources/nfs-version

      - name: Commit and push
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add resources/
          git diff --staged --quiet && { echo "No metadata changes"; exit 0; }
          git commit -m "Update build metadata [skip ci]"
          for attempt in 1 2 3; do
            if git push origin main 2>&1; then exit 0; fi
            git pull --rebase origin main 2>/dev/null || true
            sleep $((5 * attempt))
          done
          echo "::warning::Failed to push metadata after 3 attempts"

  # ===========================================================================
  # Job 7: Update README version
  # ===========================================================================
  update-readme-version:
    timeout-minutes: 15
    name: Update README
    needs: [check-releases, promote-latest, update-metadata]
    if: needs.promote-latest.result == 'success'
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update version in README
        env:
          IMAGE_TAG: ${{ needs.check-releases.outputs.image_tag }}
          NFS_VERSION: ${{ needs.check-releases.outputs.nfs_version }}
        run: |
          set -euo pipefail
          if [[ ! -f README.md ]]; then
            echo "No README.md found — skipping"
            exit 0
          fi

          sed -i "s|nfs-server-alpine:[0-9][0-9.]*-r[0-9]*|nfs-server-alpine:${NFS_VERSION}|g" README.md

          if grep -q 'COMPONENT_VERSIONS_START' README.md; then
            sed -i '/COMPONENT_VERSIONS_START/,/COMPONENT_VERSIONS_END/{
              s|<td><code>[0-9][0-9.]*-r[0-9]*</code></td>|<td><code>'"${NFS_VERSION}"'</code></td>|
            }' README.md
            echo "Updated component version table: nfs-utils=${NFS_VERSION}"
          fi

          if grep -q 'VERSION_TAGS_START' README.md; then
            sed -i '/VERSION_TAGS_START/,/VERSION_TAGS_END/{
              s|<td align="center">[0-9][0-9.]*-r[0-9]*</td>|<td align="center">'"${NFS_VERSION}"'</td>|
              s|NFS Utils build version [0-9][0-9.]*-r[0-9]*|NFS Utils build version '"${NFS_VERSION}"'|
            }' README.md
            echo "Updated version tags table: nfs-utils=${NFS_VERSION}"
          fi

      - name: Commit and push
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet && { echo "No README changes"; exit 0; }
          git commit -m "Update README with latest versions [skip ci]"
          for attempt in 1 2 3; do
            if git push origin main 2>&1; then exit 0; fi
            git pull --rebase origin main 2>/dev/null || true
            sleep $((5 * attempt))
          done
          echo "::warning::Failed to push README after 3 attempts"

  # ===========================================================================
  # Job 8: Update Docker Hub description
  # ===========================================================================
  update-dockerhub-description:
    timeout-minutes: 15
    name: Update Docker Hub Description
    needs: [update-readme-version]
    if: always() && needs.update-readme-version.result == 'success'
    runs-on: ${{ vars.ACTION_RUNNER_VERSION || 'ubuntu-latest' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Update Docker Hub description
        uses: peter-evans/dockerhub-description@v5
        continue-on-error: true
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DOCKERHUB_REPO }}
          readme-filepath: ./README.md
